# 🚀 快速启动指南（增强版自动交易系统）

## ✅ 系统已就绪

你的量化策略系统已经完全配置好了！包含以下功能：

### 核心功能
- ✅ 每小时自动生成做空信号
- ✅ 自动追踪信号表现
- ✅ 动态收集训练数据
- ✅ 自动重新训练模型
- ✅ 实时统计监控

---

## 📋 一键启动（选择你的系统）

### Windows 用户

打开 **PowerShell（管理员）**，复制粘贴以下命令：

```powershell
# 进入项目目录
cd F:\2025\Quant4Little

# 创建每小时自动任务
schtasks /create /tn "Quant4Little自动交易" /tr "F:\2025\Quant4Little\run_hourly_trading.bat" /sc hourly /st 00:00

# 立即运行一次测试
schtasks /run /tn "Quant4Little自动交易"

Write-Host "`n✅ 设置完成！系统将每小时自动运行" -ForegroundColor Green
Write-Host "查看实时日志: Get-Content logs\hourly_trading_202511.log -Tail 50 -Wait" -ForegroundColor Yellow
```

### Linux 用户

打开终端，复制粘贴以下命令：

```bash
# 进入项目目录
cd /path/to/Quant4Little

# 添加执行权限
chmod +x run_hourly_trading.sh

# 添加到crontab（每小时运行）
(crontab -l 2>/dev/null; echo "0 * * * * $(pwd)/run_hourly_trading.sh") | crontab -

# 立即运行一次测试
./run_hourly_trading.sh

echo "✅ 设置完成！系统将每小时自动运行"
echo "查看实时日志: tail -f logs/hourly_trading_202511.log"
```

---

## 📊 监控命令

### 查看实时日志

**Windows:**
```powershell
Get-Content logs\hourly_trading_202511.log -Tail 50 -Wait
```

**Linux:**
```bash
tail -f logs/hourly_trading_202511.log
```

### 查看统计仪表板

```bash
python scripts/dashboard.py
```

### 查看今日信号

```bash
# Windows
type data\paper_trading\signals_20251123.csv

# Linux
cat data/paper_trading/signals_20251123.csv
```

---

## 🎯 系统运行流程

```
每小时整点 (00:00, 01:00, 02:00 ...)
    ↓
[自动执行]
  1. Paper Trading - 生成SHORT信号
  2. 收集统计数据
  3. 追踪信号表现
  4. 更新训练数据
  5. 自动重训模型（如需要）
    ↓
[输出文件]
  - data/paper_trading/signals_YYYYMMDD.csv
  - data/paper_trading/signals_history.csv
  - data/backtest_trades.csv
  - data/statistics/stats_YYYYMM.csv
  - logs/hourly_trading_YYYYMM.log
```

---

## 📈 预期结果时间线

### 🕐 第1小时（立即）
```
✓ 生成首批SHORT信号（2-5个）
✓ 保存到 signals_20251123.csv
✓ 记录日志
```

### 📅 第1天
```
✓ 24次运行
✓ 累积 40-60个信号
✓ 开始统计数据
```

### 📆 第5天
```
✓ 开始追踪第1天信号表现
✓ 计算实际盈亏
✓ 新增训练数据
```

### 📅 第7天
```
✓ 累积 60-80个新训练样本
✓ 首次自动重训模型
✓ 准确率提升 3-5%
```

### 📊 第30天
```
✓ 累积 300+训练样本
✓ 重训 5-7次
✓ 准确率提升 10-15%
✓ 策略性能稳定
```

---

## 🔍 验证系统正常运行

### 1. 检查任务是否创建成功

**Windows:**
```powershell
schtasks /query /tn "Quant4Little自动交易"
```

**Linux:**
```bash
crontab -l
```

### 2. 检查日志文件

```bash
# 应该有最新时间戳的日志
ls -lh logs/hourly_trading_202511.log
```

### 3. 检查输出文件

```bash
# Windows
dir data\paper_trading\signals_*.csv

# Linux
ls -lh data/paper_trading/signals_*.csv
```

### 4. 运行仪表板

```bash
python scripts/dashboard.py
```

应该看到类似输出：
```
================================================================================
  Quant4Little 策略监控仪表板
  更新时间: 2025-11-23 16:00:00
================================================================================

================================================================================
  📊 信号统计
================================================================================

📈 总体数据:
  总信号数: 45
  独立标的: 12
  日期范围: 2025-11-23 ~ 2025-11-23

📅 最近7天:
  信号数: 45
  日均: 6.4
```

---

## ⚙️ 配置说明

### 数据更新时间
- **默认**: 每天早上 8:00 更新币池数据
- **其他时间**: 只运行Paper Trading，不更新数据
- **修改**: 编辑 `scripts/hourly_trading_enhanced.py` 中的 `if current_hour == 8:`

### 模型重训条件
- **条件1**: 超过24小时未训练
- **条件2**: 新增样本≥50个
- **满足任一条件即自动重训**

### 币池大小
- **当前**: 200个低流动性币种
- **修改**: 编辑 `scripts/update_latest_data.py` 中的 `bottom_symbols = ticker_df.head(200)`

---

## 🎯 下一步操作（可选）

### 1. 等待系统累积数据（推荐）
```bash
# 无需任何操作，系统会自动运行
# 每天查看一次仪表板即可
python scripts/dashboard.py
```

### 2. 手动触发一次测试

**Windows:**
```powershell
schtasks /run /tn "Quant4Little自动交易"
```

**Linux:**
```bash
./run_hourly_trading.sh
```

### 3. 监控第一周数据

```bash
# 每天运行查看统计
python scripts/dashboard.py

# 查看信号历史
cat data/paper_trading/signals_history.csv
```

---

## ❓ 常见问题

### Q: 如何停止自动运行？

**Windows:**
```powershell
schtasks /delete /tn "Quant4Little自动交易" /f
```

**Linux:**
```bash
crontab -e
# 删除包含 run_hourly_trading.sh 的行
```

### Q: 如何手动运行一次？

```bash
python scripts/hourly_trading_enhanced.py
```

### Q: 如何查看模型是否重训了？

```bash
# 查看日志中的重训记录
grep "重新训练模型" logs/hourly_trading_202511.log

# 查看模型文件修改时间
ls -lh models/rank_model.pt
```

### Q: 数据存储在哪里？

```
data/
├── paper_trading/          # 每日信号
│   ├── signals_20251123.csv
│   └── signals_history.csv
├── backtest_trades.csv     # 训练数据
├── statistics/             # 每小时统计
│   └── stats_202511.csv
├── daily_klines/           # 日线数据（200个币）
└── hourly_klines/          # 小时线数据（200个币）

logs/
└── hourly_trading_202511.log  # 运行日志

models/
├── rank_model.pt           # 模型文件
└── rank_model_meta.json    # 模型元数据
```

---

## 🎉 完成！

恭喜！你的量化策略系统已经启动，现在会：

- ✅ **每小时自动运行** - 无需人工干预
- ✅ **持续生成信号** - 捕捉做空机会
- ✅ **自动优化模型** - 越用越聪明
- ✅ **记录详细日志** - 可追溯审计

**只需等待数据累积，系统会自动越来越好！**

---

## 📞 获取帮助

- 📖 详细文档: [ENHANCED_SYSTEM.md](ENHANCED_SYSTEM.md)
- 📊 策略配置: [STRATEGY_CONFIG_SUMMARY.md](STRATEGY_CONFIG_SUMMARY.md)
- ⚙️  设置指南: [HOURLY_TRADING_SETUP.md](HOURLY_TRADING_SETUP.md)

---

祝交易顺利！📈
