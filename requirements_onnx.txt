# ECS服务器依赖 - PyTorch版本
# 适用于2CPU/1GB RAM的服务器

# 核心依赖
ccxt>=4.0.0           # 交易所API
pandas>=2.0.0         # 数据处理
numpy>=1.24.0         # 数值计算
scikit-learn>=1.3.0   # 数据预处理

# PyTorch (CPU版本，节省空间)
torch>=2.0.0          # 深度学习框架（CPU版本）

# 可选：进度条和时间处理
tqdm>=4.65.0
python-dateutil>=2.8.0
